<ul class="nav nav-list departments">
    <li class="nav-header">Choose a Department</li>

    @foreach (var department in ViewBag.Departments)
    {
        <li>
            <a href="@CatalogUrlHelper.DepartmentUrl(Html, department)" class="@(((int?)department.Id) == (int?)ViewBag.DepartmentId ? "active" : "")" title="@department.Description">&raquo;&nbsp;@department.Name&nbsp;&laquo;</a>
        </li>
    }
</ul>