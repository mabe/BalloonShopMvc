@model System.Collections.Generic.IEnumerable<BalloonShop.Model.ShoppingCart>
<div class="CartSummary">
    @if (Model.Any())
    { 
        <strong>Cart summary </strong><a href="@Url.Action("Index", "Cart")" class="CartLink">(view details)</a>
        <ul>
            @foreach (var item in Model)
            {
                <li>@item.Quantity x @item.Balloon.Name</li>
            }
        </ul>
        <hr />
        <span>Total: <span class="ProductPrice">@ViewBag.Total.ToString("c")</span></span>
    }
    else
    { 
        <strong>Your shopping cart is empty.</strong>
    }
</div>

